# TODO: Terminal UI

Add a terminal UI (TUI) to inspect, while `sftp-file-transfer-scheduled` is
running:

- Live logs
- Current status (last cycle result, next run time)
- Overall usage stats (send history summary — sent/failed/pending counts,
  akin to `sftp_history report`)
